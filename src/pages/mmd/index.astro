---
import { getCollection } from "astro:content";
import ListLayout from "../../layouts/ListLayout.astro";
import { attachMeta } from "../../utils/meta";

const posts = (await getCollection("mmd", ({ data }) => !data.draft))
    .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
    .map((p) => attachMeta(p));
---

<ListLayout
    title="MMD"
    description="MikuMikuDanceに関する制作記録や技術メモ。"
    ogImage="https://res.cloudinary.com/dellp9a4z/image/upload/v1756758389/og-blog_gnqmat.png"
    entries={posts}
/>